# --- SSH key pair, generated by Terraform (not by you). The private key
#     is stored ONLY in AWS Systems Manager Parameter Store, encrypted -
#     never a GitHub secret, never a file on your machine, never
#     committed. The deploy job fetches it at deploy time using the AWS
#     credentials it already has. ---
resource "tls_private_key" "deploy" {
  algorithm = "ED25519"
}

resource "aws_key_pair" "deploy" {
  key_name   = "${var.project_name}-deploy-key"
  public_key = tls_private_key.deploy.public_key_openssh
}

resource "aws_ssm_parameter" "deploy_private_key" {
  name  = "/${var.project_name}/deploy-ssh-key"
  type  = "SecureString"
  value = tls_private_key.deploy.private_key_openssh
}

# --- Bastion host: the only machine in this whole setup with a public
#     IP that also accepts SSH. Everything else is reached by hopping
#     through this box. ---
resource "aws_instance" "bastion" {
  ami                    = data.aws_ami.amazon_linux_2023.id
  instance_type          = "t3.micro"
  subnet_id              = aws_subnet.public[0].id
  vpc_security_group_ids = [aws_security_group.bastion.id]
  key_name               = aws_key_pair.deploy.key_name

  tags = { Name = "${var.project_name}-bastion" }
}

resource "aws_eip" "bastion" {
  instance = aws_instance.bastion.id
  domain   = "vpc"
  tags     = { Name = "${var.project_name}-bastion-eip" }
}

# --- Public entry point for the app. The app server itself has no
#     public IP, so this is the only way anyone reaches the website. ---
resource "aws_lb" "app" {
  name               = "${var.project_name}-alb"
  internal           = false
  load_balancer_type = "application"
  security_groups    = [aws_security_group.alb.id]
  subnets            = aws_subnet.public[*].id

  tags = { Name = "${var.project_name}-alb" }
}

resource "aws_lb_target_group" "app" {
  name     = "${var.project_name}-tg"
  port     = 80
  protocol = "HTTP"
  vpc_id   = aws_vpc.main.id

  health_check {
    path                = "/"
    healthy_threshold   = 2
    unhealthy_threshold = 5
    interval            = 15
    timeout             = 5
    matcher             = "200-399"
  }

  tags = { Name = "${var.project_name}-tg" }
}

resource "aws_lb_target_group_attachment" "app" {
  target_group_arn = aws_lb_target_group.app.arn
  target_id        = aws_instance.app.id
  port             = 80
}

resource "aws_lb_listener" "http" {
  load_balancer_arn = aws_lb.app.arn
  port              = 80
  protocol          = "HTTP"

  default_action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.app.arn
  }
}
